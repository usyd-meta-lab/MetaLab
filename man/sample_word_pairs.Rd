% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_word_pairs.R
\name{sample_word_pairs}
\alias{sample_word_pairs}
\title{Sample related or unrelated cue–target word pairs from association norms}
\usage{
sample_word_pairs(
  path = "Florida Norms.xlsx",
  n_pairs = 40,
  pair_type = c("related", "unrelated"),
  desired_mean = 0.47,
  desired_sd = 0.14,
  tol_mean = 0.02,
  tol_sd = 0.03,
  fas_range = c(0.3, 0.75),
  fas_unique_cutoff = 0.05,
  bsg_unique_cutoff = 0.05,
  unrelated_cutoff = 0,
  min_char = 1,
  max_char = Inf,
  max_iter = 20000,
  seed = 123,
  na_strings = c("", "NA", "N/A", "#N/A", "#N", "#NUM!", "#DIV/0!", "#VALUE!", "#REF!",
    "#NAME?", "#NULL!", "—", "–", "-", ".", "#G", "TMC")
)
}
\arguments{
\item{path}{Path to the Excel file with columns \code{cue}, \code{target}, \code{fsg}, \code{bsg}.}

\item{n_pairs}{Number of pairs to return.}

\item{pair_type}{\code{"related"} or \code{"unrelated"}.}

\item{desired_mean, desired_sd}{Target mean/SD of \code{fsg} (related mode only).}

\item{tol_mean, tol_sd}{Absolute tolerances for mean/SD (related mode only).}

\item{fas_range}{Inclusive range of eligible \code{fsg} (related mode).}

\item{fas_unique_cutoff, bsg_unique_cutoff}{Cross-talk thresholds.}

\item{unrelated_cutoff}{In unrelated mode, any association strictly greater
than this (forward/backward) counts as related and causes rejection.}

\item{min_char, max_char}{Integer bounds on allowed word length (applied to
vocabulary/rows before sampling).}

\item{max_iter}{Maximum stochastic search iterations.}

\item{seed}{RNG seed.}
}
\value{
Tibble with columns \code{cue}, \code{target}, \code{fsg}, \code{bsg} and attributes.
}
\description{
Draws a set of cue–target pairs from the Florida Free Association Norms
(or a compatible table) under constraints on association strength,
cross-talk isolation, and word length. Modes:
\itemize{
\item \strong{related}: sample rows with \code{fsg} within \code{fas_range},
matching \code{desired_mean}/\code{desired_sd}.
\item \strong{unrelated}: pair distinct words from the vocabulary and
reject if any association (forward/backward) exceeds \code{unrelated_cutoff}
within pairs or across pairs (cross-talk).
}
}
