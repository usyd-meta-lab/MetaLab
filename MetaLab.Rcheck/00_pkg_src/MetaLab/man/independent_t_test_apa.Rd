% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/independent_t_test_apa.R
\name{independent_t_test_apa}
\alias{independent_t_test_apa}
\title{Independent Samples t-test with APA Reporting}
\usage{
independent_t_test_apa(formula, data, var.equal = TRUE)
}
\arguments{
\item{formula}{A formula specifying the outcome and group variables, e.g., \code{score ~ group}.}

\item{data}{A data frame containing the variables in the formula.}

\item{var.equal}{Logical. If \code{TRUE} (default), assumes equal variances (Student's t-test).
If \code{FALSE}, Welch's t-test is used.}
}
\value{
A character string reporting the t-test results in APA format.
}
\description{
Conducts an independent-samples t-test and returns an APA-formatted result string, including group means,
standard deviations, t-statistic, p-value, and Cohen's d.
}
\details{
\itemize{
\item Checks that the grouping variable has exactly two levels.
\item Calculates group means and standard deviations.
\item Computes the t-statistic, degrees of freedom, p-value, and Cohen's d.
\item Outputs a complete APA-style sentence describing the results.
}
}
\examples{
\dontrun{
# Example dataset
mydata <- data.frame(
  score = c(85, 88, 90, 78, 82, 91, 84, 77, 86, 80),
  group = rep(c("Control", "Treatment"), each = 5)
)

# Run the function
result <- independent_t_test_apa(score ~ group, data = mydata, var.equal = TRUE)
cat(result)
}

}
